import {TonConnectButton} from '@tonconnect/ui-react';

/**
 * @returns {JSX.Element}
 */
export function TONConnect() {
    return (
        <div>
            <TonConnectButton/>
        </div>
    );
}
